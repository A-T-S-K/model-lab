{
  "name": "ai-village-model-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/model/*.test.ts tests/trace/*.test.ts tests/integration/*.test.ts tests/app/*.test.ts",
    "test:reference": "python3 -m unittest discover -s tests/reference -v && python3 reference/generate_fixture.py --check",
    "test:browser": "playwright test",
    "build": "npm run typecheck && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "acceptance": "npm run test:reference && npm run test && npm run build && npm run test:browser",
    "test:isolation": "python3 scripts/check_isolation.py"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "24.10.1",
    "tsx": "4.23.13",
    "typescript": "7.0.2",
    "vite": "8.2.2"
  }
}
