# End-to-end product and launch review

**2026-09-13 — functional route passes; hold an unguided public launch for the small corrections below and hardware/user checks.** Prepared assets start without external application requests. Candidate decisions, disposable ablation, and return to accepted state work. The main weakness is comprehension: substantial arithmetic remains inspector-led, and the compact comparison layout makes that inspector difficult to read.

Reviewed the clean, existing `wave-1a-spatial` checkout at `9e6f5c5df100cff7d64d5b66edf4f2478b4f2ab7` (also the local origin-tracking ref), tree `c6fb74591426ed8c6cfb32618e9c4c5d29940810`. No remote refresh or checkout change. Read the supplied instructions, [2C review](wave-2c-review.md), README, operations and relevant recovery documentation. Read-only runtime hashing matched all 71 source inputs to `sha256:314f2e9f144e4ea72bf98aa7e9d57d8ea17c2cc58a61ba0cecd8676f2517680e`; all three prepared JS bundles contain that revision ([asset hashes](../test-results/end-to-end-review/prepared-assets.json)). No rebuild or product-code change.

Inspected all twenty original full-size 2C PNGs linked in that review, plus the actual 117.24-second local MP4 through twelve full-resolution samples across its duration. This is sampled recording inspection, not a frame-by-frame motion certification. New screenshots, UI transcripts, recording, scripts and logs are under [test-results/end-to-end-review](../test-results/end-to-end-review/). Live UI inspection preceded implementation diagnosis. This agent review is **not unfamiliar-user comprehension testing**.

## Visitor route and reproduction

Use the prepared build with `npm run preview -- --port 4173 --strictPort` when that port is free; during this review its existing preview was left running. Use a disposable browser at 1920×1080, repeat decision/comparison views at 1280×720, and retain default input `abca`.

1. **Launch → Predict.** `/` opens the public recorded attract loop; **TOUCH TO START → Why this prediction? · Explore → Source controls and other evidence** exposes the classic inspector. It does not expose the spatial route. Open `/?presentation=spatial` explicitly, then **Predict**. It completes at step 0, position 4. The overview explains topology, two heads, parameter banks and residual connections, but initially selects attention scores rather than a labeled prediction result ([overview](../test-results/end-to-end-review/06-predict.png)).
2. **Actual arithmetic → unguided detour.** **Values / arithmetic / source → Inspect Q output scalar → multiply #8990 → layer0.attn_wq[0,7] → Raw scalar fields** reaches the actual parameter `0.04730832110527332`. The selected Q sum is `0.441144899459`; its final product is `0.047308321 × 1.834091469 ≈ 0.086767788160`. Select **ReLU** from Operation, then **Home → Back → Focus** as needed. Navigation works; the inspector states `max(0, -0.0976353) = 0`. The scene shows the gate and neighboring consumers when focused. Numeric source tracing requires inspector scrolling; it is not explained by the overview's bars alone ([parameter](../test-results/end-to-end-review/10-parameter-exact.png), [detour](../test-results/end-to-end-review/12-back.png)).
3. **Controlled learning → partial contribution → candidate.** **Step through learning → Continue → Pause when backward begins → Continue to next contribution to pinned parameter** exposes `0.071431117 × 1`, accumulated from zero, explicitly PARTIAL. The owner breadcrumb points to token embedding. **Continue** advances through proposal and candidate forward to **Candidate ready — not accepted**. The first uninterrupted pass skipped over the partial stop; the repeat used an automated click of the real Pause button at the phase boundary, as the existing test does. That precision does not demonstrate human discoverability. At Ready the complete input mean is `1.2158285451638384 → 1.0850956462725034`; selected position 4 is correctly labeled target END ([partial](../test-results/end-to-end-review/16-partial-1280.png), [Ready](../test-results/end-to-end-review/17-ready-1280.png)).
4. **Explicit decision.** Toggle **Inspect current / Inspect candidate / Shared-scale comparison**, then **Accept update**: accepted step becomes 1. A separate Ready candidate was explicitly discarded at step 0; another was discarded later at accepted step 1. Cancellation after one baseline operator also retained step 1. Provisional, accepted and historical labels are explicit; no automatic acceptance was observed.
5. **Ablation → downstream → unchanged accepted model.** Select **Weighted values**, **Head 1**, then **Test without this head**. The read-only banner declares all-position replacement after aggregation/before concat. Inspect **Attention softmax**: exact upstream equality is visible in the table. Return to Weighted values and follow the inspector's downstream buttons through **Combined heads → Attention projection → Attention residual → Pre-MLP RMSNorm → MLP up → ReLU → MLP down → MLP residual → Output logits → Output probabilities**. Concat channels 0–3 stay equal; 4–7 become zero. The input mean changes to `1.0907998396726164`; END probability rises from `0.3718062834652309` to `0.3774929966686526`, demonstrating why one token's increase is not overall improvement. **Inspect selected scalar** requests the selected arm; switching to **Inspect baseline** clears it. **Return to current model → Predict** restores the accepted view. Full snapshot equality, not just the step label, passed after ablation and again after cancellation/discard: `sha256:0a43d3e90da2adb442be0d8fbdc18984c11414f72d43e7bc4ccb3d5ae1565712` ([summary](../test-results/end-to-end-review/manual-summary.json), [HTTP evidence](../test-results/end-to-end-review/manual-http-audit.json), [return](../test-results/end-to-end-review/26-returned-1280.png)). Passive worker auditing began late in the accepting transaction; its command counts do not cover the earlier exploratory route.

## Prioritized corrections — maximum five

| Priority / category | Evidence-backed problem and exact trigger | Smallest correction |
| --- | --- | --- |
| **P1 · correctness defect (rendering)** | Follow step 2 to a scalar, or step 5 to the ablated probability scalar. **Forward result** is nearly invisible: computed foreground `rgb(242,245,247)` on `rgb(242,245,237)`. Raw fields still contain the value. [Capture](../test-results/end-to-end-review/10-parameter-exact.png). Diagnosis after viewing: the global `.equation` light background survives inside the dark spatial microscope. No numerical defect established. | Scope an explicit dark equation background/readable foreground to the spatial scalar inspector; verify both live and reconstructed arithmetic visually. |
| **P1 · product / entry** | Start at `/`, follow Explore and expand source controls: no spatial entry. The requested controlled learning route requires knowing the query URL. Later source inspection confirms the toggle is gated by that initial query. [Expanded UI transcript](../test-results/end-to-end-review/04-source-controls.txt). | Make the spatial entry reachable from the existing public controls, preserving the current session; explicitly label the controlled learning path. Until then, launch directly at the documented spatial URL. |
| **P1 · product / compact readability** | At 1280×720, reach Ready or a head comparison with the inspector open. Ready leaves about 263 vertical pixels for the scene; the proposed-Adam card becomes miniature. Ablation tables push Inputs/Calculation/Result below the fold. Controls remain available, but presence is not readability. [Ready](../test-results/end-to-end-review/17-ready-1280.png), [head](../test-results/end-to-end-review/19-head-1280.png). The 1920 views are substantially clearer, although detailed tables still require scrolling. | Adjust existing compact spacing/type and focus framing so the selected calculation remains readable; keep the decision controls and selected arm visible. No new renderer or layout system. |
| **P2 · product / prediction meaning** | Predict in spatial mode, then select Output probabilities. The overview uses `P`/vertex IDs and component numbers; the inspector initially emphasizes component `[0]`, not the token with highest probability. Ready supplies a much clearer token table. [Returned output](../test-results/end-to-end-review/26-returned-1280.png). | Reuse explicit token names and selected-position result labeling in the existing output view; clarify the shared START/END marker. Preserve exact values in details/tooltips. |
| **P2 · product / learning stop discovery** | **Step through learning → Continue**, without intervention, reaches Ready without a readable partial-gradient stop. The pinned-contribution action only becomes relevant during backward; first-time visitors must anticipate that phase. | Add a short instruction beside the existing Continue control explaining how to stop at the pinned contribution. Recheck whether unfamiliar visitors can use it before changing execution behavior. |

## Operations, recovery and remaining limits

**No operational blocker reproduced in the tested prepared-build environment.** [Cold-start script/results](../test-results/end-to-end-review/cold-start.json) used separate fresh persistent Chromium profiles for public 4173 and spatial 4174. The latter server was newly started with the existing preview command. Browser request routing denied every non-loopback host; service workers were blocked and browser-local DNS rules rejected external resolution. A deliberate external fetch failed in each profile. Local assets returned 200, needed bundled fonts loaded, the model worker started, and Predict completed without page errors or external application requests. Observed ready times were 96/84 ms and completed checks 133/360 ms; these single local samples are not performance targets. The first harness run expected the wrong displayed rounding (`0.345307` versus `0.3453065`); only that evidence-script assertion was corrected, then both fresh profiles passed. No system network setting changed.

**Recovery verification:** four existing tests passed in 18.4 seconds on `playwright.spatial.config.ts` (4174), covering selected/canonical Reset with retained history, input/presentation/Clear cancellation, paused/history and synthetic hidden-tab handling, archive-admission retry, worker failure/restart, and cancelled/failed/replaced ablations with full accepted-state preservation. [Log](../test-results/end-to-end-review/recovery-tests.log). Reproduce:

```sh
WAVE2C_EVIDENCE_DIR=test-results/end-to-end-review/automated-2c WAVE2B_EVIDENCE_DIR=test-results/end-to-end-review/automated-2b npx playwright test --config playwright.spatial.config.ts tests/browser/spatial-wave2c.spec.ts tests/browser/spatial-wave2b.spec.ts tests/browser/model-lab-v2.spec.ts tests/browser/spatial-wave2a.spec.ts --grep 'cancelled, failed|T06 controlled|distinct reset intents|E07 HTTP' --workers=1 --output=test-results/end-to-end-review/recovery-tests
```

Run that configuration only when 4174 is free. The full 4173/4174 suites were not rerun; 4173 received the interactive route and cold-browser check. The original 4173 preview remained untouched; the review-owned 4174 server and browser were closed afterward.

**Untested operational limits:** no offline dependency installation/build, OS cold boot/cache eviction, actual process crash durability, prolonged soak, archive-budget exhaustion, maximum-input sweep, or real five-minute Exhibit idle/foreground cycle. Transactions/history remain in memory; reload is not a resume mechanism. Synthetic worker/visibility failures are narrower than OS termination or sleep. Exact values are accessible through raw fields and titles, but touch-only and assistive-technology access was not certified.

**Requires actual hardware or unfamiliar users:** target display/DPI, touch hit areas and hover alternatives, physical viewing distance/lighting, camera motion comfort, real background/resume and sustained kiosk operation. Separately ask unfamiliar visitors to identify the input/target, explain one operation from the scene before opening the inspector, take a detour and return, distinguish partial from final gradient, deliberately accept/discard, and explain why ablation did not train the model. No such testing was conducted here.

**Smallest next change set:** fix scalar contrast, expose the existing spatial entry, and tighten the existing 1280 comparison presentation; include only the two small labeling/instruction corrections above. Then repeat this bounded route and the outstanding hardware/user checks. No redesign, infrastructure or mathematical-kernel batch is indicated. This review changes only this document and ignored evidence; no commits, publication, workshop changes or fixes. Stop for review.
