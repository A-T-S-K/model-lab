# ABQ overnight review · requalification in progress

The normal `wave-1a-spatial` checkout started clean at `568eaaa03c2d000999a9ac46ea915a35c74787cf`. Product and kit implementation are committed locally. No branch/worktree/clone, support-tree edit, remote write, merge, deployment, dependency upgrade, machine setting or workshop change was made.

## Delivered implementation

ON1 binds a compatible retained recording into the opt-in spatial kiosk, with static **RECORDED RUN · REPLAY / Recorded real run. Not live.** Start consumes the activation and requests a fresh prediction through the existing owner. The short route explicitly selects the `abca` q3/head0/key0 example, exposes prediction and attention landmarks, preserves detours, and restores the saved guide selection only on Resume. Ordinary root and direct spatial entries remain available.

Public reset clears presentation history, camera, pin, inspector, experiment and guide state along with the existing worker/archive reset. It restores playback defaults and Home. Warning interactions preserve the Keep-session target; wheel activity joins pointer/touch/keyboard activity. Candidate decisions remain explicit. The initial 300-second/20-second policy is configurable and can be disabled for facilitation.

ON2 adds a coordinated scene construction below the selected map operation. It reuses complete-vector Q/K geometry, signed products, complete causal stable softmax, all-key affine value mixture and a distinct concat → WO → saved-residual handoff. Exact full values and source remain in the inspector. Existing ReLU and live contribution/Adam constructions are retained. No model, scheduler, renderer, generation or persistence framework was added.

ON3 corrects standalone README paths and supplies a dependency-free loopback launcher and read-only prepared artifact. Admission refusals for controlled start and head comparison now give actionable messages. The kit contains local fonts, workers, notices, exact startup instructions and hashes; Node 24+ must already be installed.

## Previous release identity · awaiting scoped comparison-layout repair

- Implementation checkpoint: `7f9bc151c4044e8e60f6666d2f5e3cc56a28d292`; tree `d2682564a5c1330423637d59d8c48678158b4790`.
- Runtime: `sha256:28353b1e94e995f76cf75429bcf0145508511c76d9a668ff855d88a54c865923`, 72 production inputs.
- Prepared kit: `test-results/abq-overnight/release/`, 16 hashed payload files / 794071 bytes, plus manifest.
- Manifest SHA-256: `e2bcf2a1bd8efa79ad383c6517840c9e4fa984bd4d450f73523bb6409e6d898a`.
- Full source/artifact/launcher/preparer identities: [identity](../test-results/abq-overnight/identity.json), [manifest](../test-results/abq-overnight/artifact-manifest.json).

Rehearsal-script and documentation edits after this checkpoint do not change the runtime or kit. The final delivery identity must record them separately.

## Current checks · do not treat this draft as release approval

Reference: 17 pass, no differing floats. Unit/integration: 127 pass. Example/typecheck/build: pass. Final HTTP4173: 109/109 pass (6.0m). Final HTTP4174: 37/37 pass (5.6m), including every required later suite. Cold artifact launch, source/scalar/worker use, explicit candidate discard, head test and separate emulated-touch route pass. Mixed-workload probe passes in 70.26 seconds with one maximum outstanding permit and clean post-reset archive. Real soak and paced media remain in progress; final duration/status will be recorded before handover.

The pre-final full 4173 run had 108 passes and one real regression: the enlarged selection card covered `wte`. Its compact action-row repair passed the unchanged retained no-match route. Earlier warning, layout and fixture findings are preserved in [failures and repairs](../test-results/abq-overnight/failures-and-repairs.md). Assertions and numerical tolerances were not weakened.

## Operator and human gates

Use the [operator runbook](abq-operator-runbook.md), [facilitator script](abq-facilitator-guide.md), and [morning checklist](morning-checklist.md). The actual event machine/display, physical touch/reach/glare, OS sleep/reboot, real background/resume and unfamiliar-user understanding remain NOT RUN. Automated expert timings and reading pauses are not participant results.

Generative continuation, additional models, broader experiment editing, durable recovery and event deployment remain outside this overnight scope. Engineering rehearsal is distinct from facilitated-exhibit and unattended-walk-up approval. Stop for human review after completing the overnight qualification.
