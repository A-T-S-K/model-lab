# M2 integrated interaction qualification

## Disposition and identity

**M2 = COMPLETE — scoped engineering qualification.** This closes only the
topology/world/interaction separation gate. It does not grant full FP-01–FP-12,
M5, M6, release, workshop, or unfamiliar-user acceptance, and it does not implement
FP-03 generation/cache or begin M3.

- Starting local and remote commit: `28aa81355ff8d1fe4c59d34693a2fed0e6d3683f`.
- Qualified source commit: `86a940ddec9ede5bf403d8d49213cf307442c3eb`
  (`Qualify M2 interaction boundaries`).
- Qualification-record commit: the documentation-only descendant containing this
  report; it does not change the qualified runtime.
- Application runtime: `sha256:2d6aded839c393c78e21f48b80c4d8bda553ad12092f3aa196e7bd5843d69b0f`.
- Pythia profile/runtime: `pythia-14m-cpu-f32-eager-v1` /
  `sha256:290becead8351ebb54ab765c1f49091e3837209fb183909e457f6240687cd298`.
- MLP witness runtime: `sha256:3c46331962a9d82220938e272fd7af50a7e547edbb4b450dac7a5985a7f99dd5`.
- Worktree caveat: pre-existing untracked `docs/abq-product-review.md` was never
  opened for edit, staged, or changed.

## Fresh integrated review

The portfolio remains one application shell with integration-owned specialization.
Canonical and multilayer MicroGPT share the topology-driven MicroGPT composition;
MLP and Pythia use registered presentations; grouped, shape-only and opaque evidence
share the bounded fallback. Store, player, archive, session coordination, semantic
identity and replay admission remain shared.

The model-ID search found IDs only in registration, executors, strict integration
codecs/profiles, integration-owned presentations and tests. No M2 ID branch occurs in
the generic evidence store, player, archive or session coordinator. Historical change
surface confirms that M2-A established the shared boundary, M2-B made the substantive
shared correction needed by the first non-transformer presentation, M2-C added the
fallback with four small shared call-site changes, and M2-D added Pythia through
registration/specialization without copying the app/session/store/navigation shell.
A hypothetical new integration is bounded to its descriptor, codec/executor binding,
capabilities, optional presentation, registration and tests unless it exposes a real
generic contract deficiency.

Semantic addresses remain anchored to model definition, semantic node, port, run,
invocation, phase and integration-owned typed coordinates. Tests covered MicroGPT
layer/head/position, MLP example/features, distinct grouped query/KV spaces, Pythia
QKV/attention/output index, stale-run refusal and incompatible switch invalidation.
No second universal selection type was added.

## Defects and repairs

Three bounded interaction defects were repaired:

1. The shared inspector allowed an already-selected alternate run to open while a
   canonical paced transaction was active, hiding the canonical transaction. The
   world action is now disabled and defensively refused until the canonical operation
   finishes or is cancelled.
2. MLP rendered a Home button without a handler. Home now selects the model-local
   numeric input, clears incompatible coordinates through the existing selector, and
   restores visible keyboard focus.
3. Full public/session reset relied on archive replacement to make an alternate world
   disappear but retained its run/replay/selection identifiers internally. Reset now
   explicitly clears those identifiers before rebuilding canonical/exhibit state.

No store, player, archive, topology redesign, native binding, fixture, tolerance or
golden changed.

## Integrated behavior

The fresh browser route exercised:

`canonical → multilayer → MLP → grouped → shape-only → opaque → Pythia → canonical → MLP replay → Pythia replay → multilayer replay`.

It retained a real accepted canonical update and the same selected learning experiment
across the tour; alternate worlds exposed no canonical candidate, Adam, intervention,
lesson or scalar-continuation controls. A new paced canonical prediction blocked world
entry and execution until cancellation. MLP Home was model-local and focus-visible.
The reduced-motion return had no stale lens or alternate coordinates. The final replay
phase issued zero native HTTP requests and no additional multilayer worker request;
canonical browser-local Predict still completed afterward.

Multilayer remains read-only. MLP exposes retained prediction/training/SGD evidence but
no candidate adoption. Fallback fixtures remain inspection/replay only and preserve
available, shape-only, opaque and unsupported meanings without placeholder numbers.
Pythia remains inference/capture/replay only; blocks 0 and 2–5 are visibly uncaptured,
pre-RoPE QKV does not become a post-RoPE score reconstruction, all 50,304 logits remain
the probability support, omitted mass remains visible, and index 50,303 is exact.

Existing receipt, delayed response, stale selection, failed publication, direct legacy
import, reset epoch, replay offset and archive identity tests all passed. Canonical
Ready/Accept and separate Discard passed, as did Wave 1D hidden-tab/delayed-inspection
and keyboard detour routes. Exhibit fake-time expiry, Keep session, facilitated opt-out
and public reset returned to a safe canonical entry without accepting a candidate.

## Readable code, visual and accessibility review

Canonical MicroGPT forward/backward/training remains native TypeScript, MLP remains
readable PyTorch, and Pythia remains native Hugging Face execution with read-only hooks.
Generic composition describes and binds evidence; it does not interpret model math.

Fresh 1920×1080 captures were inspected for canonical overview and learning gradient,
multilayer layer-1/head-2 detail, MLP SGD, opaque refusal, Pythia overview and full-support
output 50,303. The 1280×720 reduced-motion canonical return was also inspected. Hierarchy,
selection/focus, capability/refusal text and numeric availability remained legible; no
critical control overlap, clipping, stale lens or unavailable-data encoding was found.
Keyboard activation/focus was exercised for MLP Home, MicroGPT/Pythia semantic objects,
Wave 1D learning detours, exhibit entry and the inspector focus trap. This was scoped
engineering accessibility inspection, not a full screen-reader or WCAG audit.

## Commands and results

- `npm run typecheck` — PASSED.
- `WITNESS_RECORDING_DIR=… NATIVE_RECORDING=… npm test` — PASSED, 165/165,
  zero skipped.
- `npm run test:reference` — PASSED, 17 tests and strict conformance, zero differing
  floats.
- `npm run test:reference:canonical` — PASSED, byte-exact regeneration.
- `npm run example` — PASSED.
- `research/pythia/.venv/bin/python research/witnesses/qualify.py` — PASSED,
  70 comparisons, maximum absolute error 0, nine refusals; fresh output
  `test-results/scratch/native-1r4d9nin`.
- `research/pythia/.venv/bin/python research/pythia/qualify.py` — PASSED at
  `atol=1e-6`, `rtol=1e-6`, all reported maximum errors 0; fresh output
  `test-results/scratch/native-zfy7d2af`.
- `research/pythia/.venv/bin/python research/pythia/test_bridge.py` against explicit
  task ports — PASSED, 10/10 transport guards.
- `python3 tests/integration/native-output.test.py -v` — PASSED, 2/2.
- `vite build --outDir test-results/scratch/m2e-build-candidate-1/build` — PASSED;
  original `dist` was not touched.
- Protected M2-E route — PASSED, 1/1; fresh final evidence
  `test-results/scratch/m2e-browser-new-4`.
- Protected retained M2-A/M2-C/Wave-1A/shared-repair matrix — PASSED, 9/9;
  `test-results/scratch/m2e-browser-matrix-1`.
- Protected canonical Ready/Accept/Discard and Wave-1D lifecycle/keyboard subset —
  PASSED, 3/3; `test-results/scratch/m2e-browser-canonical-1`.
- Protected exhibit reset/expiry subset — PASSED, 2/2;
  `test-results/scratch/m2e-browser-exhibit-1`.

The first sandboxed `npm test` attempt had only two localhost `EPERM` failures and
passed unchanged with loopback permission. One M2-E attempt asserted a downstream
learning element before requesting its inspection; the route was corrected to perform
the real inspection and passed on a fresh output. A mistyped unittest module name
failed to import; the repository test file then passed directly. These attempts did
not change product criteria or evidence bytes.

## Explicitly unrun

The full historical browser catalog, aggregate `npm run acceptance`, installation
isolation, benchmarks, soaks, complete ABQ six-state reset matrix, full accessibility
audit and release/workshop/user qualification were not run: the focused candidate
covered affected M2 boundaries and representative retained routes without transferring
later-stage or release claims. Live MLP and Pythia HTTP browser executions were not
repeated after their fresh native qualifications because no native binding/profile
changed; fresh recordings were replayed through the exact candidate. M3–M6 and the
unfinished FP portfolio were not begun.
